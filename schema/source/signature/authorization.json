{
  "$id": "authorizationSignature",
  "type": "object",
  "required": [
    "authorization",
    "channel"
  ],
  "additionalProperties": false,
  "properties": {
    "authorization": {
      "type": "string",
      "minLength": 6
    },
    "channel": {
      "type": "string",
      "enum": ["sms", "email"]
    }
  }
}
